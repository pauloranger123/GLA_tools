<Window x:Class="GLAtools.ManageMembersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gerenciar Membros" Height="500" Width="380"
        Background="{StaticResource BrushBgDarkest}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Membros da Aliança" Style="{StaticResource TextTitle}" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="PlayersItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="36"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource WoodTextBox}"/>
                            <Button Grid.Column="1" Content="✕" Margin="6,0,0,0"
                                    Click="DeleteMember_Click" Tag="{Binding}"
                                    ToolTip="Remover membro">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="#5C1A1A" CornerRadius="3"
                                                            BorderBrush="{StaticResource BrushGoldBorder}" BorderThickness="1">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                          TextElement.Foreground="White"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Adicionar Membro" Style="{StaticResource WoodButtonSecondary}"
                    Margin="0,0,8,0" Click="AddMember_Click"/>
            <Button Content="Fechar" Style="{StaticResource WoodButton}" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
